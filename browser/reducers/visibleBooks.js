// import {HAVE_READ, TO_READ, FAVORITES} from './visibilityFilterReducer';

// const getVisibleBooks = (books, filter) => {
// 	switch (filter) {
// 		case HAVE_READ:
// 			return books.filter( book => book.toRead === 'no');
// 		case TO_READ:
// 			return books.filter( book => book.toRead === 'yes');
// 		case FAVORITES:
// 			return books.filter( book => book.starrred === 'yes');
// 		default:
// 			return books;
// 	}
// };

// export default getVisibleBooks;
